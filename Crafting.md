# 合成宝袋配方记录工具 —— 需求与开发记录

## 一、项目背景

《以撒的结合：忏悔+》中，里该隐的合成宝袋需要放入 8 个掉落物来合成道具。玩家需要反复尝试不同组合，并记录已经使用过的配方，避免重复。最初从一个纯数学问题开始，逐步演变为一个带图形界面的配方记录工具，并探索自动化获取游戏内数据的可能性。

---

## 二、初始数学问题

### 2.1 四种物品，总数 8

- 物品可区分，每种数量不限，允许某种选 0 个。
- 问：总数为 8 的不同选择方法有多少种？
- 答案：\(\binom{8+4-1}{4-1} = \binom{11}{3} = 165\) 种。
- 若要求每种至少选 1 个，则为 \(\binom{7}{3} = 35\) 种。

### 2.2 五种物品，总数 8

- 允许某种选 0 个：\(\binom{12}{4} = 495\) 种。
- 每种至少选 1 个：\(\binom{7}{4} = 35\) 种。

### 2.3 思路

- 设每种数量为 \(x_i\)，解 \(x_1 + x_2 + \dots + x_n = 8\)。
- 使用隔板法：8 个物品排成一排，插入 \(n-1\) 个隔板。
- 允许 0 个时隔板可以相邻或在两端；每种至少 1 个时隔板只能插在中间空隙。

---

## 三、配方记录工具的需求演变

### 3.1 最初需求：记录已用过的组合

- 用户询问：如果一种组合方式已经使用过，该怎么记录下来以防重复？
- 讨论方案：
  - 用集合（Set）存元组
  - 编码成整数
  - 数据库或 Redis
  - 预生成所有组合后打乱

### 3.2 针对《以撒的结合》合成宝袋

- 合成宝袋规则：8 个槽位，26 种可用掉落物，顺序无关。
- 合成结果受“总权重”和“游戏种子”影响。
- 记录配方时需要标准化：固定物品顺序，只记录数量大于 0 的物品。
- 推荐格式：`金币3+红心2+钥匙3`。

### 3.3 简化记录需求

- 用户明确：**仅仅记录使用过的配方**，不考虑种子、权重、合成结果。
- 核心操作：把配方标准化成唯一字符串，然后查重。

### 3.4 手动输入太麻烦

- 用户觉得一个一个输入很麻烦。
- 讨论方案：
  - 游戏内 Mod `Crafting Companion` 自动同步到外部计算器。
  - 截图 + OCR 识别。
  - 使用缩写（如 `C3+H2+K3`）快速录入。

### 3.5 EID 模组

- EID（External Item Descriptions）内置合成宝袋助手。
- 功能：
  - 预测当前配方会合成什么。
  - 搜索特定道具的配方。
  - 记录本局已合成过的配方（Learned Recipes）。
- 局限：记录仅限当前一局，新局重置。

---

## 四、Python 记录脚本的开发

### 4.1 第一版：命令行脚本

- 输入四位数字，如 `0350`，分别代表心、硬币、钥匙、炸弹的数量。
- 总和必须为 8。
- 未存储则追加到 `combinations.txt`；已存储则提示。
- 使用集合查重，文件持久化。

### 4.2 持久化与连续输入

- 用户希望持续存储，而不是每次重新运行脚本。
- 解释：数据已保存在文件，脚本循环输入，退出后下次运行自动加载。
- 提供 Tkinter GUI 版本，窗口可一直开着，回车即记录。

### 4.3 添加删除记录功能

- 增加“清空记录”按钮，确认后删除 `combinations.txt` 并清空列表。

### 4.4 物品范围扩展到 28 种

- 用户提供 28 种掉落物的名称与品质。
- 要求删除“注释”列、删除“空槽位”“屎块”“其它”三行。
- 后又要求删除“图标”列。
- 生成 `合成宝袋组件.xlsx`，包含“名称”“品质”两列。
- 用户手动添加“顺序”列，用于标准化配方。

### 4.5 基于 Excel 顺序的 GUI 脚本

- 读取 `合成宝袋组件.xlsx`，按“顺序”排列按钮。
- 左键点击增加数量，右键减少。
- 总数达到 8 后可记录，自动查重。
- 记录格式：`红心3+硬币2+钥匙3`。
- 持久化到 `combinations.txt`。

### 4.6 使用 uv 管理环境

- 用户希望在该文件夹下直接创建环境。
- 步骤：

  ```bash
  uv init
  uv venv
  uv add openpyxl
  uv run combo_recorder.py
  ```

- 脚本头部的 `# /// script` 依赖声明可去掉。

### 4.7 界面优化

- 去掉记录成功后的提示窗口。
- 去掉窗口置顶功能。
- 调整窗口大小，使内容居中、按钮整齐。
- 初始窗口大小从 760x1060 逐步调整为 700x820，后又因图标调整至 720x960。

### 4.8 跨平台字体适配

- 用户自行修改代码，动态选择系统可用字体：
  - 中文字体候选：Microsoft YaHei、PingFang SC、Noto Sans CJK SC、WenQuanYi 等。
  - 等宽字体候选：Consolas、DejaVu Sans Mono、Noto Sans Mono 等。
- 所有控件使用 `FONT_*` 变量，并设置 `root.option_add("*Font", FONT_NORMAL)`。

### 4.9 加入种子输入

- 参考 `seed.py`，实现种子字符串与 32 位种子值互转。
- GUI 增加种子输入框，校验通过后切换到该种子的记录文件。
- 文件命名：`combinations_XXXX_XXXX.txt`。
- 无种子时使用 `combinations.txt`。

### 4.10 加入道具 ID 与中文名

- 参考 `以撒的结合忏悔+_全道具信息表.xlsx`。
- 记录配方后，可选择道具 ID，保存格式：

  ```text
  配方|道具ID|道具中文名
  ```

- 例如：`红心1+硬币2+钥匙3+炸弹2|118|硫磺火`
- 提供道具搜索框，支持中文名、英文名、ID 模糊匹配。
- 增强检索：显示匹配数量、回车选中第一个、双击选中、Esc 清空、精确匹配自动选中。

### 4.11 显示掉落物图标

- 用户提供 sprite sheet 图片 `Crafting_ui_sprite.png`。
- 图片尺寸 128×128。
- 结构：
  - 顶部 4 行：第一行从第 2 格开始（7 个），第二、三行各 8 个，第四行前 5 个（金硬币、金胶囊、金电池等），共 28 个。
- 每个图标 16×16。
- 使用 Pillow 切图，按钮显示图标 + 数字。
- 去掉生成预览图功能。
- 去掉合成界面掉落物的名字，只保留图标和数字。

### 4.12 快速启动

- 提供 `.bat` 或 `.vbs` 放桌面，双击启动。
- `.bat` 内容示例：

  ```bat
  @echo off
  set "PROJECT=C:\Users\czy\Desktop\github-repositories\Tainted-Cain"
  cd /d "%PROJECT%"
  start "" "%PROJECT%\.venv\Scripts\pythonw.exe" "%PROJECT%\combo_recorder.py"
  ```

- `.vbs` 无黑框版本。

---

## 五、功能需求汇总

### 5.1 核心功能

- ☑ 输入 8 个掉落物组合（通过按钮增减数量）。
- ☑ 总数必须为 8 才能记录。
- ☑ 按 Excel 中的“顺序”标准化配方字符串。
- ☑ 查重：同一配方不能重复记录。
- ☑ 记录内容：配方、道具 ID、道具中文名。
- ☑ 持久化存储，每个种子独立文件。
- ☑ 支持删除当前种子的记录文件。
- ☑ 显示已记录列表。

### 5.2 种子支持

- ☑ 输入种子字符串，校验合法性。
- ☑ 切换种子时自动加载对应记录文件。
- ☑ 显示当前种子及其 32 位值。

### 5.3 道具检索

- ☑ 按中文名、英文名、ID 模糊搜索。
- ☑ 显示匹配数量。
- ☑ 回车选中第一个结果。
- ☑ 双击列表项选中。
- ☑ Esc 清空检索。
- ☑ 精确匹配唯一项时自动选中。
- ☑ 已选道具高亮。

### 5.4 界面与交互

- ☑ 窗口居中、固定初始大小。
- ☑ 跨平台字体适配。
- ☑ 物品按钮显示图标和数量。
- ☑ 按钮左键 +1，右键 -1。
- ☑ 顶部显示总数和当前配方。
- ☑ 操作按钮：记录配方、清空当前、删除记录文件。
- ☑ 已记录列表带滚动条。

### 5.5 数据文件

- `合成宝袋组件.xlsx`：名称、品质、顺序。
- `以撒的结合忏悔+_全道具信息表.xlsx`：道具 ID、英文名、中文名、品质等。
- `seed.py`：种子字符串与 32 位值互转。
- `Crafting_ui_sprite.png`：掉落物图标精灵图。
- `combinations*.txt`：记录文件。

---

## 六、自动化探索

### 6.1 目标

- 让脚本自动获得配方和合成道具结果，避免手动记录。

### 6.2 现有工具

- EID + Repentogon：EID 的合成宝袋助手在 Repentogon 支持下可 100% 准确计算，自动记录本局已学配方。
- Crafting Companion：按下 G 键自动将当前房间掉落物和种子发送给外部计算器。
- 合成宝袋挖掘器：网页工具，支持批量输入和记录多个配方。

### 6.3 自行开发

- 用户希望基于 Repentogon API 自行开发。
- 结论：没有 Repentogon，无法实现自动获取配方和合成结果。
- 原版 Lua API 没有直接查询合成宝袋产出的函数。
- Repentogon 提供 800+ 新 API，可访问底层合成逻辑，挂钩合成事件。
- 官方文档：https://repentogon.com/docs.html

---

## 七、技术栈

- Python 3
- Tkinter（GUI）
- openpyxl（读写 Excel）
- Pillow（处理图标）
- uv（环境与依赖管理）
- Repentogon（未来自动化方向）

---

## 八、当前状态

- 配方记录工具已具备：种子输入、道具检索、图标显示、持久化存储、查重、删除记录。
- 自动化获取游戏内数据尚未实现，需依赖 Repentogon API 进一步开发。
- 工具可通过桌面 `.bat` 或 `.vbs` 快速启动。

---

## 九、待办与未来方向

- □ 研究 Repentogon API，尝试编写 Lua 模组自动捕获配方和产物。
- □ 将自动捕获的数据写入 Python 脚本可读取的文件，实现无缝记录。
- □ 考虑与 EID 的“已学配方”功能整合或互补。
- □ 优化界面，增加更多便捷操作（如导入/导出记录）。
