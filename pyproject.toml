[project]
name = "tainted-cain"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Gold_pps", email = "3061215992@qq.com" }
]
requires-python = ">=3.13"
dependencies = [
    "openpyxl>=3.1.5",
    "pillow>=12.3.0",
]

[project.scripts]
tainted-cain = "tainted_cain:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
